---
description: Forces the AI agent to sync with the current roadmap and agent communications.
---
1. Use the `view_file` tool to read `roadmap.md` in the root directory to understand the current priority phase.
2. Use the `view_file` tool to read `agent_comms.md` in the root directory to read the latest handoffs from the other AI agent.
3. Acknowledge what tasks have been claimed or handed off, and state what your immediate next objective is based on those files.
